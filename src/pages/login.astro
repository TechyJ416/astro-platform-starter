---
// src/pages/login.astro
export const prerender = false;

import Layout from "../layouts/Layout.astro";
import { createClient } from "@supabase/supabase-js";

const session = Astro.locals.session;
const profile = Astro.locals.profile;

// If already logged in, redirect to appropriate dashboard
if (session && profile) {
  const redirectUrl = Astro.url.searchParams.get('redirect');
  if (redirectUrl) {
    return Astro.redirect(redirectUrl);
  }
  
  if (profile.role === 'admin') {
    return Astro.redirect('/admin');
  } else if (profile.role === 'manager') {
    return Astro.redirect('/manager/dashboard');
  } else {
    return Astro.redirect('/creator/dashboard');
  }
}

let errorMessage = '';
let successMessage = '';

if (Astro.request.method === 'POST') {
  const formData = await Astro.request.formData();
  const email = formData.get('email') as string;
  const password = formData.get('password') as string;
  const redirectUrl = formData.get('redirect') as string;

  if (!email || !password) {
    errorMessage = 'Please enter both email and password.';
  } else {
    const supabase = createClient(
      import.meta.env.PUBLIC_SUPABASE_URL,
      import.meta.env.PUBLIC_SUPABASE_ANON_KEY
    );

    const { data, error } = await supabase.auth.signInWithPassword({
      email,
      password
    });

    if (error) {
      errorMessage = error.message;
    } else if (data.session) {
      // Set auth cookies
      const cookieOptions = {
        path: '/',
        httpOnly: true,
        secure: import.meta.env.PROD,
        sameSite: 'lax' as const,
        maxAge: 60 * 60 * 24 * 7 // 7 days
      };

      Astro.cookies.set('sb-access-token', data.session.access_token, cookieOptions);
      Astro.cookies.set('sb-refresh-token', data.session.refresh_token, cookieOptions);

      // Get user profile to determine redirect
      const adminClient = createClient(
        import.meta.env.PUBLIC_SUPABASE_URL,
        import.meta.env.SUPABASE_SERVICE_KEY,
        { auth: { autoRefreshToken: false, persistSession: false } }
      );

      const { data: profile } = await adminClient
        .from('profiles')
        .select('role')
        .eq('id', data.user.id)
        .single();

      // Determine redirect URL
      let finalRedirect = redirectUrl || '/dashboard';
      
      if (!redirectUrl && profile) {
        if (profile.role === 'admin') {
          finalRedirect = '/admin';
        } else if (profile.role === 'manager') {
          finalRedirect = '/manager/dashboard';
        } else {
          finalRedirect = '/creator/dashboard';
        }
      }

      return Astro.redirect(finalRedirect);
    }
  }
}

const redirectParam = Astro.url.searchParams.get('redirect') || '';
const registered = Astro.url.searchParams.get('registered');
if (registered) {
  successMessage = 'Account created! Please sign in.';
}
---

<Layout title="Sign In">
  <div class="auth-page">
    <div class="auth-container">
      <div class="auth-card">
        <div class="auth-header">
          <a href="/" class="logo">🚀 Banity</a>
          <h1>Welcome Back</h1>
          <p>Sign in to your account</p>
        </div>

        {errorMessage && (
          <div class="alert error">{errorMessage}</div>
        )}
        
        {successMessage && (
          <div class="alert success">{successMessage}</div>
        )}

        <form method="POST" class="auth-form">
          <input type="hidden" name="redirect" value={redirectParam} />
          
          <div class="form-group">
            <label for="email">Email</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required 
              placeholder="you@example.com"
              autocomplete="email"
            />
          </div>

          <div class="form-group">
            <label for="password">Password</label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              required 
              placeholder="••••••••"
              autocomplete="current-password"
            />
          </div>

          <button type="submit" class="btn btn-primary btn-block">
            Sign In
          </button>
        </form>

        <div class="auth-footer">
          <p>Don't have an account? <a href={`/register${redirectParam ? `?redirect=${redirectParam}` : ''}`}>Create one</a></p>
          <a href="/forgot-password" class="forgot-link">Forgot password?</a>
        </div>
      </div>
    </div>
  </div>

  <style>
    .auth-page {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #1a1a2e 100%);
      padding: 2rem;
    }

    .auth-container {
      width: 100%;
      max-width: 420px;
    }

    .auth-card {
      background: white;
      border-radius: 20px;
      padding: 2.5rem;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }

    .auth-header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: #1a1a2e;
      text-decoration: none;
      display: inline-block;
      margin-bottom: 1.5rem;
    }

    .auth-header h1 {
      font-size: 1.5rem;
      color: #1a1a2e;
      margin: 0 0 0.5rem 0;
    }

    .auth-header p {
      color: #6b7280;
      margin: 0;
    }

    .alert {
      padding: 0.875rem 1rem;
      border-radius: 10px;
      margin-bottom: 1.5rem;
      font-size: 0.875rem;
    }

    .alert.error {
      background: #fee2e2;
      color: #991b1b;
      border: 1px solid #fecaca;
    }

    .alert.success {
      background: #d1fae5;
      color: #065f46;
      border: 1px solid #a7f3d0;
    }

    .auth-form {
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .form-group label {
      font-size: 0.875rem;
      font-weight: 500;
      color: #374151;
    }

    .form-group input {
      padding: 0.875rem 1rem;
      border: 2px solid #e5e7eb;
      border-radius: 10px;
      font-size: 1rem;
      transition: all 0.2s;
    }

    .form-group input:focus {
      outline: none;
      border-color: #2563eb;
      box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
    }

    .btn {
      padding: 0.875rem 1.5rem;
      border-radius: 10px;
      font-weight: 600;
      font-size: 1rem;
      border: none;
      cursor: pointer;
      transition: all 0.2s;
    }

    .btn-primary {
      background: linear-gradient(135deg, #2563eb, #3b82f6);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(37, 99, 235, 0.3);
    }

    .btn-block {
      width: 100%;
    }

    .auth-footer {
      margin-top: 2rem;
      text-align: center;
    }

    .auth-footer p {
      color: #6b7280;
      margin: 0 0 0.75rem 0;
    }

    .auth-footer a {
      color: #2563eb;
      text-decoration: none;
      font-weight: 500;
    }

    .auth-footer a:hover {
      text-decoration: underline;
    }

    .forgot-link {
      font-size: 0.875rem;
    }
  </style>
</Layout>
